/**
 * @velos/iam-react — React bindings for Velos IAM.
 *
 * Phase 0 scaffold. VelosProvider, hooks (useUser/useAuth/useClient) and the
 * prebuilt components (<SignIn/>, <SignUp/>, <UserButton/>) land in Phases 3–4.
 */
export const VELOS_REACT_VERSION = "0.0.0";

export type { VelosUser, VelosSession, AppAccess } from "@velos/iam-core";
