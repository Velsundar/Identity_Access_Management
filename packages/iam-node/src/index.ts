/**
 * @velos/iam-node — server SDK for Velos IAM.
 *
 * Phase 0 scaffold. verifyToken (via JWKS), requireAuth() middleware and the
 * admin API client land in Phase 5.
 */
export const VELOS_NODE_VERSION = "0.0.0";

export type { VelosUser, AppAccess, RouteRule } from "@velos/iam-core";
